---
- name: Create manifests directory on first master
  become: true
  file:
    path: /var/lib/rancher/k3s/server/manifests
    state: directory
    owner: root
    group: root
    mode: 0644
  run_once: true
  when: "'kube-0' in ansible_play_hosts and play_hosts.index('kube-0') == 0"

- name: Download vip rbac manifest to first master
  become: true
  ansible.builtin.get_url:
    url: "https://raw.githubusercontent.com/kube-vip/kube-vip/{{ kube_vip_tag_version }}/docs/manifests/rbac.yaml"
    dest: "/var/lib/rancher/k3s/server/manifests/vip-rbac.yaml"
    owner: root
    group: root
    mode: 0644
  run_once: true
  when: "'kube-0' in ansible_play_hosts and play_hosts.index('kube-0') == 0"

- name: Copy vip manifest to first master
  become: true
  template:
    src: "vip.yaml.j2"
    dest: "/var/lib/rancher/k3s/server/manifests/vip.yaml"
    owner: root
    group: root
    mode: 0644
  run_once: true
  when: "'kube-0' in ansible_play_hosts and play_hosts.index('kube-0') == 0"